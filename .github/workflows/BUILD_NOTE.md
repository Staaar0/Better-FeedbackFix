# Build note

This patch kit is meant to be copied into a CS2Fixes fork.

Use CS2Fixes' own build workflow / Docker build.

CS2Fixes currently documents Docker and manual AMBuild compilation in its README.
Do not make a separate fake workflow until the patch is integrated into a real CS2Fixes fork.
